import styled from "styled-components";

// Component Div
export const TabDiv = styled.div.attrs({
  className: "md:flex flex-col w-full md:col-start-2 md:col-span-1 hidden",
})``;

// UL for Tabs
export const Ul = styled.ul.attrs({
  className:
    "overflow-scroll w-auto flex flex-col self-center p-4 md:text-right text-center",
})``;
